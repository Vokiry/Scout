name: soulseek_protocol
version: 0.1.0
description: Pure Dart implementation of the Soulseek peer-to-peer file sharing protocol.
repository: https://github.com/vokiry/soulseek-flutter
publish_to: none
resolution: workspace

environment:
  sdk: ^3.12.0

dependencies:
  meta: ^1.16.0
  collection: ^1.19.0

dev_dependencies:
  test: ^1.25.2
  lints: ^5.0.0
